#include<bits/stdc++.h>
using namespace std;
//字符串原地空格替换，从后面开始替换可以节约移动次数
void replaceblank(char string[], int length)
{
    //首先数有多少个空格
    if (string==nullptr||length<=0)
        return;
}
int main()
{
    string word;
    word = "hello world";
    for (int i = 0; i < 20;i++)
    {
        if(word[i]=='\0'){
            cout << i << endl;
        break;}
            
    }
        
    return 0;
}